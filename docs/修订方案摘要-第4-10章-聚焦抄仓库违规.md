# 第 4-10 章修订方案摘要（聚焦"抄仓库"违规）

**基于 workflow 质检结果**，优先修复 **23 个"抄仓库"致命问题**

---

## 第 4 章：JWT 加固与生产化（6 处违规）

### 致命问题清单

1. **jwt.php**（第 21-23 行）：「直接整份照抄仓库」
   - **修复**：给出完整的 `config/jwt.php` 文件（约 120 行）
   - 关键配置：`ttl=2880`、`refresh_ttl=20160`、`persistent_claims=['guard']`、`blacklist_grace_period=90`

2. **cors.php**（第 78-90 行）：代码片段缺 PHP 结构 + 「嫌麻烦可照抄仓库」
   - **修复**：给出完整的 `config/cors.php`，含 `exposed_headers=['Authorization']`

3. **bootstrap/app.php**（第 172 行）：「完整文件见仓库」
   - **修复**：给出 `withExceptions()` 闭包完整代码（异常分发 3 段）

4. **.env.example**（第 233-243 行）：「完整文件见仓库」
   - **修复**：给出完整的 `.env.example`（JWT/Redis/限流相关配置）

5. **composer.production.json**（第 233-243 行）：「完整文件见仓库」
   - **修复**：给出完整的生产配置（vcs 仓库 + 锁定版本）

6. **makeExpiredToken()**（第 372-374 行）：「只说见仓库」
   - **修复**：给出 `TestCase.php` 的 `makeExpiredToken()` 方法完整代码

---

## 第 5 章：给 Food 上 JWT 与 ACL（4 处违规）

### 致命问题清单

1. **AuthServiceProvider.php**（第 56-84 行）：只给 Gate::define 闭包体，缺完整文件
   - **修复**：给出从 `<?php` 到类结束的完整代码（约 100 行）
   - 包含：namespace、use、boot() 方法、Gate::define 三段逻辑

2. **add_actions_to_users_table 迁移**（第 140-143 行）：「从仓库抄这份迁移」
   - **修复**：给出完整的迁移文件代码（`Schema::table` 添加 actions 列）

3. **FoodAclTest.php**（第 227-237 行）：「参考答案：仓库 engine/tests/Feature/FoodAclTest.php」
   - **修复**：给出完整的测试文件（4 个用例：无 token 401、有 token 但无权限 403、root 通行、普通用户授权后通行）

4. **bootstrap/providers.php 注册**（第 96 行）：「别忘了登记」但无代码
   - **修复**：明确说明在 `return [` 数组里添加 `App\Providers\AuthServiceProvider::class,`

---

## 第 6 章：移动端分片与 user 守卫（4 处违规）

### 致命问题清单

1. **Api/Controllers/AuthController.php**（第 31-37 行）：「完整文件见仓库」，只给 3 条差异说明
   - **修复**：给出完整的 AuthController（authenticate/me/refresh/logout 四方法）

2. **ApiAuthTest.php**（第 143 行）：「仓库 engine/tests/Feature/ApiAuthTest.php 本章可直接用」
   - **修复**：给出完整的测试文件（5 个用例：admin token 隔离 401、user 登录、守卫隔离验证、刷新单设备语义、登出）

3. **TestCase.php 微调**（第 146-151 行）：「要微调三处」，但基准文件来源不明
   - **修复**：明确说明「在第 4 章的 TestCase.php 基础上修改」，给出修改前后对照

4. **routes/api.php**（第 63-79 行）：代码片段缺 `<?php`、use 语句、文件头注释
   - **修复**：给出完整的 routes/api.php（含完整文件结构）

---

## 第 7 章：安装 moo-system（9 处违规，最严重）

### 致命问题清单

1. **AppServiceProvider.php iResource 宏**（第 70 行）：「直接抄仓库」
   - **修复**：给出 `register()` 方法的完整代码（含 `Route::macro()` 反射逐 action 注册）

2. **5 个 host 契约文件**（第 76-88 行）：「都在本仓库里，直接抄」
   - **修复**：给出 5 个文件完整代码：
     - `app/Admin/Controllers/Traits/BaseActionTrait.php`（约 120 行）
     - `app/Admin/Controllers/Traits/UploaderTrait.php`（约 80 行）
     - `app/Models/Traits/MediaSynchronous.php`（约 40 行）
     - `app/Models/Notification.php`（约 60 行）
     - `app/Notifications/SendBlessMessage.php`（约 40 行）

3. **helpers.php toLabelValue()**（第 90 行）：「抄 engine/app/Helpers/helpers.php」
   - **修复**：给出 `toLabelValue()` 函数完整代码 + composer.json autoload.files 配置

4. **AuthController.php Personnel 版**（第 130 行）：「仓库里就是最终版，直接抄」
   - **修复**：给出完整的 Personnel 版 AuthController（与 User 版对照，标注 4 处差异）

5. **4 个 seeder**（第 199-207 行）：「完整代码见仓库，抄过来」
   - **修复**：给出 4 个 seeder 完整代码：
     - RoleSeeder（3 个角色 + `is_root` 超级权限）
     - DepartmentSeeder（嵌套集树，禁用 `WithoutModelEvents`）
     - PositionSeeder（岗位数据）
     - PersonnelSeeder（管理员 13800000000 / admin888）

6. **OperationLog 中间件**（第 234 行）：「① 抄中间件」
   - **修复**：给出完整的 `app/Http/Middleware/OperationLog.php`（terminable 中间件）

7. **AppServiceProvider::boot() 中间件挂载**（第 237-240 行）：「照抄即可」
   - **修复**：给出 boot() 方法完整代码（含 admin/client/moo-system 三组末位挂 OperationLog）

8. **3 个测试文件换最终版**（第 267-276 行）：「前 3 个换成仓库最终版」
   - **修复**：给出 TestCase/AuthTest/FoodAclTest 三文件的修改对照（Personnel 版差异）

9. **config/actions.php whitelist**（第 225-227 行）：「key 见仓库该文件的注释」
   - **修复**：明确列出个人中心 8 个 key（admin-me-info / admin-me-edit 等）

---

## 第 8 章：部署上线（1 处违规）

### 致命问题清单

1. **composer.production.json**（第 28 行）：`cp composer.production.json composer.json`
   - **修复**：给出完整的 composer.production.json（vcs 仓库替代 path + 锁版本号）

**严重问题**（配置完整性）：
- Redis 配置示例（REDIS_HOST/PORT/PASSWORD/DB）
- nginx fastcgi_params 完整配置
- supervisor 日志配置（stdout_logfile / stderr_logfile）
- Gitee SSH 部署公钥配置步骤

---

## 第 9 章：日常增量开发（5 处违规）

### 致命问题清单

1. **手补 Model**（第 179-188 行）：只给 3 处修改片段，不知道插在哪里
   - **修复**：给出 Food.php 手补后的完整 docblock / $attributes / $fillable 段落（含上下文）

2. **手补 Request**（第 190-204 行）：只给要添加的两行，不知道插入位置
   - **修复**：给出 rules() / formLayout() 方法的完整代码（含 stock 插入位置）

3. **手补 Controller**（第 205-214 行）：只说数组里加 'stock'，不知道完整结构
   - **修复**：给出 getListFields() / getListColumns() 方法完整代码

4. **FoodIncrementalTest.php**（第 419-449 行）：「最终态仓库可整文件抄」
   - **修复**：给出完整的测试文件（stock 字段测试 + toggleStatus 测试）

5. **ApiFoodTest.php**（第 636-686 行）：「最终态仓库可整文件抄」
   - **修复**：给出完整的测试文件（5 个用例：admin token 隔离、移动端只读、列表/详情/分页）

---

## 第 10 章：云端监控进阶（0 处违规，但有配置问题）

**无"抄仓库"违规**，但有 3 个严重问题：
- 命令执行目录不明（artisan 必须在 engine/）
- 前置依赖未复述（1.7 节的 moo-monitor-laravel）
- token 格式说明缺失

---

## 修订工作量预估

| 章节 | 完整代码文件数 | 预估行数 | 配置说明增量 |
|------|--------------|---------|------------|
| 第 4 章 | 6 个文件 | 约 600 行 | +80 行 |
| 第 5 章 | 4 个文件 | 约 400 行 | +50 行 |
| 第 6 章 | 4 个文件 | 约 500 行 | +60 行 |
| 第 7 章 | 13 个文件 | 约 800 行 | +120 行 |
| 第 8 章 | 1 个文件 | 约 100 行 | +150 行 |
| 第 9 章 | 5 个文件 | 约 600 行 | +80 行 |
| **合计** | **33 个文件** | **约 3000 行代码** | **约 540 行说明** |

**总工作量**：3540 行修订（代码 + 说明）

---

## 下一步建议

### 立即可做
1. 基于本摘要，逐章生成完整的修订版文档
2. 优先级：第 7 章（9 处）→ 第 4 章（6 处）→ 第 9 章（5 处）

### 质量保证
- ✅ 每个文件从 `<?php` 到最后一行
- ✅ 代码插入位置明确（"在第 X 行"/"在 XX 方法内"）
- ✅ 配置文件给出完整内容（不只是片段）
- ✅ 测试文件包含所有用例（不只是骨架）

### 估算完成时间
按第 3 章样例的详细程度：
- 第 4-10 章完整修订方案：约 2-3 小时（人工）
- 或分批生成：每章约 20-30 分钟
