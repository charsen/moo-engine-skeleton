# docs/index.html 同步修改清单

**基于第 1-3 章修订方案**，需要同步修改 `docs/index.html` 的章节描述（sub 字段）

---

## 修改 1：第 1 章 sub 描述 - 监控改为可选（对应第 1 章 P0-1）

### 当前内容（第 196 行）
```javascript
{ id: 'ch1',    file: '01-安装-laravel.md',            title: '第 1 章 安装 Laravel 12', sub: '建项目 · 连库 · 真机访问 · 1.7 接入监控' },
```

### 修改为
```javascript
{ id: 'ch1',    file: '01-安装-laravel.md',            title: '第 1 章 安装 Laravel 12', sub: '建项目 · 连库 · 真机访问 · 1.7【可选】监控' },
```

**理由**：与第 1 章修订方案保持一致，1.7 节改为可选（需要私有仓库访问权限）

---

## 检查：其他章节是否需要修改

检查第 2-10 章的 `sub` 描述，确认是否有需要同步的地方：

### 第 2 章（第 197 行）
```javascript
{ id: 'ch2',    file: '02-安装-moo-scaffold.md',       title: '第 2 章 安装 moo-scaffold', sub: '开源代码生成器 · 生成 foods · 调试器' },
```
✅ **无需修改** - 描述准确，未提及已删除的"坑"

### 第 3 章（第 198 行）
```javascript
{ id: 'ch3',    file: '03-JWT-登录认证-自建用户.md',    title: '第 3 章 JWT 登录认证', sub: '自建 User · 零付费依赖' },
```
✅ **无需修改** - 描述准确

### 第 4-10 章
✅ **暂时无需修改** - 等 Batch 2-4 workflows 完成后再确认

---

## 可选优化：LS_KEY 版本号

### 当前内容（第 207 行）
```javascript
const LS_KEY = 'moo-skeleton-tutorial-v2';   // 2026-06 章节重排后步序全变，升版重置旧进度
```

### 可选修改（如果第 1-3 章步骤顺序有重大变化）
```javascript
const LS_KEY = 'moo-skeleton-tutorial-v3';   // 2026-06-13 质检后修订：第 1-3 章步骤调整，升版重置旧进度
```

**是否修改？** 
- ✅ 修改：如果第 1-3 章的步骤顺序有重大调整（如 1.7 监控移到末尾、2.1 增加依赖安装步骤），用户旧进度可能对不上新步骤
- ❌ 不修改：如果只是文案和代码完整度改进，步骤顺序基本不变

**建议：暂不修改**，等第 1-3 章实际应用修订方案后，如果步骤结构确实有变再升版。

---

## 修改汇总

| 行号 | 字段 | 当前值 | 修改为 | 理由 |
|-----|------|--------|--------|------|
| 196 | ch1.sub | `1.7 接入监控` | `1.7【可选】监控` | 监控改为可选章节 |
| 207 | LS_KEY | `v2` | `v2`（暂不改） | 等实际应用后确认 |

---

## 注意事项

**index.html 是单文件应用**（HTML + CSS + JS 内联），修改后需要：
1. 刷新浏览器缓存（Ctrl+F5 / Cmd+Shift+R）
2. 如果用户已有进度记录，localStorage 里的 `moo-skeleton-tutorial-v2` 键会保留旧进度

**验证方式**：
```bash
cd docs && php -S 127.0.0.1:9999
# 浏览器打开，检查第 1 章描述是否显示"【可选】"
```

---

## 下一步

✅ 三层同步修改清单已全部生成：
1. `docs/修订方案-第1章-关键修订清单.md`
2. `docs/修订方案-第2章-关键修订清单.md`
3. `docs/修订方案-样例-第3章完整代码补全.md`
4. `docs/同步修改-README.md-清单.md`
5. `docs/同步修改-index.html-清单.md`（本文件）

🔄 等待 Batch 2-4 workflows 完成（第 4-10 章质检）
