# 第 1 章修订指令清单（精准到行号）

**原文件**：`docs/01-安装-laravel.md`（约 300 行）  
**修订原则**：从零开始，不依赖仓库文件

---

## 修订 1：MariaDB 密码设置后立即验证（行 31-37）

### 当前内容（第 31-37 行）
```markdown
> **MariaDB 新装后必做**：Homebrew 新装的 MariaDB 默认走 unix socket 认证、
> `root` 没有密码，直接照抄后文的 `mysql -uroot -p7777 -h127.0.0.1` 会认证失败。
> 先给 `root` 设一个密码（教程示例用 `7777`）：
>
> ```bash
> sudo mysql -uroot -e "ALTER USER 'root'@'localhost' IDENTIFIED BY '7777'; FLUSH PRIVILEGES;"
> ```
```

### 修改为
```markdown
> **MariaDB 新装后必做**：Homebrew 新装的 MariaDB 默认 root 账号走 unix socket 认证
> （通过操作系统用户身份免密登录），Laravel 连接走 TCP（127.0.0.1:3306）必须用账号+密码。
> 先给 root 设一个密码（教程示例用 `7777`）：
>
> ```bash
> sudo mysql -uroot -e "ALTER USER 'root'@'localhost' IDENTIFIED BY '7777'; FLUSH PRIVILEGES;"
> ```
>
> **立即验证**密码是否生效（如果这一步失败，后续 1.3 建库会卡死）：
>
> ```bash
> mysql -uroot -p7777 -h127.0.0.1 -e "SELECT VERSION();"
> # 期望输出：包含 mariadb.org binary distribution 字样 + 版本号（如 12.x.x-MariaDB）
> ```
>
> **如果验证失败**，按以下步骤排查：
> 1. 重试密码设置命令（可能未生效）
> 2. 确认 `localhost` vs `127.0.0.1` 的区别：`ALTER USER 'root'@'localhost'` 只对 localhost 生效，
>    连 `127.0.0.1` 需要 `ALTER USER 'root'@'127.0.0.1'` 或 `'root'@'%'`
> 3. 如果仍然失败，检查 MariaDB 错误日志：`brew services log mariadb`
```

---

## 修订 2：删除 Node 预检（行 44）

### 当前内容（第 41-46 行）
```bash
php -v            # 8.2 起即可，本教程用 8.3.31（8.2 / 8.3 的区别见下方说明）
composer --version
node -v && npm -v # 本章用不到 Node——这是为后续章节的前端资源构建（可选）做预检
mysql --version   # MariaDB 12.x 客户端
```

### 修改为
```bash
php -v            # 8.2 起即可，本教程用 8.3.31（8.2 / 8.3 的区别见下方说明）
composer --version
mysql --version   # MariaDB 12.x 客户端
```

**同时删除**第 53-57 行的数据库验证命令（已前置到修订 1）

---

## 修订 3：配置块警告前置（行 86-92）

### 当前内容（第 86 行）
```markdown
## 1.4 配置 .env 连接数据库
```

### 修改为
```markdown
## 1.4 配置 .env 连接数据库（取消注释，不要追加）
```

### 当前内容（第 88-92 行，在代码块之前）
```markdown
`create-project` 已经自动生成了 `engine/.env`（`APP_KEY` 也已自动填好，不用再
`key:generate`），但它默认用 SQLite，要改成连本机数据库。注意一个容易疑惑的点：
**本机装的是 MariaDB，但 `DB_CONNECTION` 写的是 `mysql`**——MariaDB 与 MySQL
协议完全兼容，用 `mysql` 驱动连 MariaDB 是惯例写法（本机若装的是 MySQL 8 则更不用改）。
编辑 `engine/.env`，改成下面这样：
```

### 修改为
```markdown
`create-project` 已经自动生成了 `engine/.env`（`APP_KEY` 也已自动填好，不用再
`key:generate`），但它默认用 SQLite，要改成连本机数据库。

**⚠️ 操作方法：找到并取消注释，不要在文件末尾追加！**

默认 `.env` 里 `DB_CONNECTION=sqlite`，而 `DB_HOST` / `DB_PORT` / `DB_DATABASE` / 
`DB_USERNAME` / `DB_PASSWORD` 这几行是被 `#` 注释掉的——请**搜索定位到这几行，
取消注释并改成下面的值**。若只在末尾追加新值，文件里会同时留着注释掉的旧行，
日后排查容易看花眼。

注意一个容易疑惑的点：**本机装的是 MariaDB，但 `DB_CONNECTION` 写的是 `mysql`**——
MariaDB 与 MySQL 协议完全兼容，用 `mysql` 驱动连 MariaDB 是惯例写法（本机若装的是 MySQL 8 则更不用改）。

编辑 `engine/.env`，找到并修改成下面这样：
```

**删除**第 106-109 行的重复警告（已前置）

---

## 修订 4：补全 9 张表完整名称（行 141）

### 当前内容（第 141 行）
```markdown
应能看到 `users`、`cache`、`jobs`、`migrations`、`sessions` 等 9 张表。
```

### 修改为
```markdown
应能看到 **9 张表**（按字母序）：`cache`、`cache_locks`、`failed_jobs`、`job_batches`、
`jobs`、`migrations`、`password_reset_tokens`、`sessions`、`users`。
```

---

## 修订 5：1.7 节改为可选（行 169-176）

### 当前内容（第 169 行标题）
```markdown
## 1.7 接入监控（本骨架标准件）
```

### 修改为
```markdown
## 1.7 【可选】接入 moo-monitor-laravel（监控先行）
```

### 在第 170 行之前插入
```markdown
> **本节定位**：监控虽是骨架标准件，但需要访问私有仓库（当前未公开），暂时拿不到源码的读者可**整节跳过**。
> 
> - **跳过影响**：第 2-9 章功能不受影响（moo-scaffold 自带 monitor 作为传递依赖会再次尝试安装），
>   只是暂时没有 `storage/moo-monitor/` 的异常档案馆。
> - **补装时机**：包发布到 Packagist 后，一行 `composer require charsen/moo-monitor-laravel` 即可补装。
> - **不跳过的前置**：需克隆私有仓库到同级目录（见下方说明）。
>
```

### 修改第 183-186 行
```markdown
**前置条件**：包源码已克隆在与本仓库**同级**的目录（`../../moo-monitor-laravel`）。
拿不到源码的读者，本节只能「读通」、跑不起来——包发布后一行
`composer require charsen/moo-monitor-laravel` 即可。
```

### 修改为
```markdown
**仓库信息**（需访问权限）：
- 地址：`https://gitee.com/charsen/moo-monitor-laravel.git`（当前私有）
- 克隆：在 `wwwroot/` 父目录下执行 `git clone https://gitee.com/charsen/moo-monitor-laravel.git`
- 鉴权：SSH key 或 HTTPS + Gitee 账号，需联系作者申请协作者权限
- 目录结构：
  ```
  wwwroot/
  ├── moo-engine-skeleton/     ← 本仓库
  │   └── engine/
  └── moo-monitor-laravel/      ← 必须克隆到这里
  ```

**拿不到源码怎么办**：本节暂时无法继续，等包发布到 Packagist 后可用 `composer require charsen/moo-monitor-laravel` 一键安装。

**有源码的继续**：
```

---

## 修订 6：方式 A→B 转轨提示（在第 61 行之后插入）

### 在第 61 行（PHP/数据库版本说明之后）插入
```markdown
**如果你是从方式 A 转到方式 B**（克隆了仓库又想从头跟教程）：根目录已有 `engine/`，
请先 `mv engine engine.backup` 或删除，否则 1.2 的 `composer create-project` 会因目录非空报错。
```

---

## 修订 7：laravel 安装器说明优化（行 64）

### 当前内容（第 64 行）
```markdown
本机没有 `laravel` 安装器，直接用 Composer 创建，并指定目录为 `engine`：
```

### 修改为
```markdown
本教程直接用 `composer create-project`（不需要全局安装 laravel/installer），并指定目录为 `engine`：
```

---

## 汇总：需要修改的行号范围

| 修订 | 行号 | 操作 | 内容量 |
|------|------|------|--------|
| 1 | 31-37 | 替换+插入 | +20 行 |
| 2 | 44 | 删除 | -1 行 |
| 2b | 53-57 | 删除 | -5 行 |
| 3 | 86-109 | 重组 | 净增 +10 行 |
| 4 | 141 | 替换 | 净增 +1 行 |
| 5 | 169-186 | 重组+插入 | 净增 +20 行 |
| 6 | 61 后 | 插入 | +3 行 |
| 7 | 64 | 替换 | 0 行 |

**预估净增行数**：+48 行（修订后约 348 行）

---

## 执行方式选择

**方式 A**：我生成完整的修订版文件（348 行）
- 优点：一次到位
- 缺点：全文替换，diff 查看不便

**方式 B**：你按本清单手动修改原文件
- 优点：精准修改，git diff 清晰
- 缺点：需要逐条操作

**方式 C**：我生成 7 个独立的 patch 文件
- 优点：可逐个 review 和应用
- 缺点：需要工具支持

**你倾向哪种方式？**
