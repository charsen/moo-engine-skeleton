# CLAUDE.md

本仓库的完整协作、教程一致性、生成边界和验证规则统一维护在 [`AGENTS.md`](./AGENTS.md)。开始任务前必须完整阅读并遵守它，本文件不维护第二份重复规则。

特别提醒：

- 先读 `notes.md`，再读对应教程章节与 `engine/` 现码。
- 本仓是公开教程 + 参考骨架 + 初始化器，不是生产 host；文档、完成态代码与初始化器必须同步。
- `Food`、双 JWT guard、可选 `moo-system` 和开发/生产双 Composer 形态都有教学语义，不能按普通应用随意裁剪。
- 网页引导器仅验收 1024 CSS px 及以上桌面视口。
