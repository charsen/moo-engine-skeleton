# 快速修订执行清单（第 1-3 章 + README + index.html）

**目标**：消除所有"抄仓库"表述，补充完整代码

---

## 第 3 章：5 处"抄仓库"违规（最优先）

### ❌ 第 43 行：删除"完整文件见仓库，直接抄"

**当前**：改造 `app/Models/User.php`（**完整文件见仓库，直接抄**），核心就两个接口方法：

**改为**：改造 `app/Models/User.php`，完整代码如下（含第 5 章 ACL 的 actions 列前置）：

然后在第 62 行的代码块之后**插入完整的 User.php**（参考 `docs/修订方案-样例-第3章完整代码补全.md` 第 21-88 行）

---

### ❌ 第 64-68 行：删除"必须照抄"+"配套迁移也在仓库"

**删除整段**，改为：

> 第 5 章会给 User 加 `actions` 列（ACL 最小授权存储）——为了让下面的 UserSeeder 能运行，
> 现在先建好这个列。**新建迁移文件**（时间戳用当前时间，如 `2024_01_15_100000`）：

然后**插入完整的迁移文件代码**（参考样例文档第 93-115 行）

---

### ❌ 第 70-73 行：删除"抄仓库"+"📦警告"

**当前**：建第一个用户（`database/seeders/UserSeeder.php`，抄仓库），并把 `DatabaseSeeder`...

**改为**：**新建文件** `database/seeders/UserSeeder.php`，完整代码如下：

然后**插入完整的 UserSeeder.php**（参考样例文档第 120-147 行）

---

### ❌ 第 124-130 行：删除"完整代码在仓库，直接抄"

**改为**：需要 3 个中间件，完整代码如下：

然后依次插入三个中间件的完整代码（参考样例文档第 174-284 行）：
1. `JWTAssignGuard.php`（约 35 行）
2. `JWTGuardAuth.php`（约 40 行）
3. `JWTAuthOrRefresh.php`（约 50 行）

---

### ❌ 第 212 行：删除"📦仓库里是 Personnel 最终版"

**当前**：写 `app/Admin/Controllers/AuthController.php`——本章是 **User（email 登录）版**，
完整代码如下（📦 第 7 章会把主体换成 Personnel，仓库里是 Personnel 最终版，所以这一份照着敲或复制下面的）：

**改为**：**新建文件** `app/Admin/Controllers/AuthController.php`，完整代码如下：

（后续代码保持不变，已经是完整的）

---

## 第 1 章：7 处修订（参考 `docs/修订指令-第1章-精准到行号.md`）

1. **行 31-37**：MariaDB 密码验证前置（+20 行）
2. **行 44**：删除 Node 预检
3. **行 86**：标题改为"1.4 配置 .env 连接数据库（取消注释，不要追加）"
4. **行 88-92**：配置块警告前置
5. **行 141**：补全 9 张表名称
6. **行 169**：标题改为"1.7【可选】接入 moo-monitor-laravel"
7. **行 183-186**：仓库信息改为完整克隆说明

---

## 第 2 章：13 处修订（参考 `docs/修订方案-第2章-关键修订清单.md`）

**最关键的 3 处**：
1. **P0-1**：第 35 行之前插入"前置条件（私有仓库访问）"说明
2. **P0-2**：第 54 行改为"合并前后对照"（不是整个替换）
3. **P1-1**：第 87 行之后插入 `composer require tucker-eric/eloquentfilter godruoyi/php-snowflake`

---

## docs/README.md：7 处修订

1. **行 39**：Node 说明改为"教程第 1-10 章均不涉及"
2. **行 42**：私有包补充克隆命令
3. **行 77**：第 1 章描述改为"1.7【可选】接入监控"
4. **行 97-98**：删除坑 1、2（已前置为正常步骤）
5. **行 99-124**：所有后续坑序号前移 2 位

---

## docs/index.html：1 处修订

**行 196**：
```javascript
{ id: 'ch1', file: '01-安装-laravel.md', title: '第 1 章 安装 Laravel 12', sub: '建项目 · 连库 · 真机访问 · 1.7【可选】监控' },
```

---

## 完整代码文件清单（第 3 章需要补充的）

在 `docs/修订方案-样例-第3章完整代码补全.md` 里找对应行号：

1. **User.php**（行 21-88）
2. **add_actions_to_users_table 迁移**（行 93-115）
3. **UserSeeder.php**（行 120-147）
4. **DatabaseSeeder.php**（行 152-163）
5. **JWTAssignGuard.php**（行 174-209）
6. **JWTGuardAuth.php**（行 214-254）
7. **JWTAuthOrRefresh.php**（行 259-309）
8. **routes/admin.php 完整版**（行 351-368）
9. **AppServiceProvider.php boot() 中间件注册**（行 317-339）

---

## 预估工作时间

- 第 3 章：30 分钟（8 个文件，复制+调整）
- 第 1 章：15 分钟（7 处修订）
- 第 2 章：20 分钟（13 处修订）
- README + index.html：5 分钟

**总计：约 70 分钟**

---

## 验证清单

修订完成后检查：
- ✅ 第 3 章无"抄仓库"/"完整文件见仓库"
- ✅ 所有代码文件从 `<?php` 到最后一行
- ✅ README.md 坑序号已重新编号
- ✅ index.html ch1.sub 已更新
